# 发布

流程已经改成**填好一切然后存草稿**，由人在草稿箱里检查后手动发布。自动发布不做——省下的两分钟不值它的脆弱度，人工过一眼也是内容质量的最后一道闸。

完整规程在 skill 里，不在这里重复，避免两份文档漂移：

**[`.claude/skills/xhs-draft/SKILL.md`](../../.claude/skills/xhs-draft/SKILL.md)**

在 Claude Code 会话里说「把第 N 篇存草稿」即可触发。

## 人工兜底

浏览器这条路挂了随时可以放弃。`scripts/xhs/output/<日期>/<序号>/` 里的 PNG 和 `note.json` 本身就是成品，AirDrop 到手机手动发，五分钟的事。
